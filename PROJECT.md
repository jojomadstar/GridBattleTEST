# 脈息・演武場：共用專案交接

> 唯一持續維護的設計、實作與交接文件，供 Codex、Claude 及其他協作者共用。
> 最後核對：2026-09-06。依本機原始碼、現有測試及使用者最新需求整理。
> 本文件是專案記錄，不取代使用者新指示；歷史提案不等於已實作或已獲授權。

## 1. 先讀這裡

- 這是參考 One Step From Eden 的即時格子戰鬥卡牌原型，以普攻命中集氣、抽牌、施放技能為核心。
- 目前保留武俠水墨場景，用原生 Canvas 2D 畫角色與戰鬥；不要自行改用 Three.js 或生成貼圖。
- 拳師「同一橫列才鎖定、瞬移到敵人左格出拳、回原位、命中集氣 90」已於 2026-09-05 隨 `e66bacf` 部署。
- 最新已驗證的遊戲版本為 `e66bacf`：Pages 工作成功，六個遊戲／預覽檔案與本機一致；後續文件提交不改動遊戲程式。
- 使用者已要求本批上傳；共用文件與兩份原文封存一併備份至 GitHub，後續改動仍不自動推送。
- 開工先看 `git status`、本節、相關規格與實際函式；結束時更新本文件的現況、測試及部署狀態。
- 2026-09-06 已完成全音效重製：46 個事件、72 個原創程序生成 WAV，待本次部署驗證；使用者明確要求測完直接上傳，不需再確認。玩法與傷害數值不變。

## 2. 共同維護約定

1. 新需求與既有記錄衝突時，依使用者最新明確指示處理，並記下被取代的規則。
2. 程式碼是「實際怎麼運作」的依據；本文件記錄「目前約定與原因」。兩者不符時明列差異，不假裝已修復。
3. 每次完成改動，在對應現況小節更新規格，再於第 9 節留短紀錄：日期、改動、實測結果、未完成事項、部署與否。
4. 測試分為「本次重跑」「先前驗證」「歷史模型聲稱」。不能把舊模擬數字當成本次測試或真人平衡結論。
5. 不另開模型專屬進度 MD。`AGENTS.md`、`CLAUDE.md` 只保留入口；`README.md` 只保留啟動導覽。
6. 兩個模型不可同時寫同一批程式或本文件。並行時先分工不同檔案，由一位整合；動筆前重新讀檔與 diff，不覆蓋另一位的未提交改動。
7. 不為符合舊提案而擅改集氣、攻速、血量、卡牌成本或職業玩法。涉及新設計的待辦需另行確認。
8. 文件不存帳號憑證、Token 或私人設定。推送只包含本次需要的檔案，保留未採用素材與使用者其他改動。
9. 寫函式名與測試命令，不依賴容易過期的行號、檔案行數或缺乏重現腳本的勝率。

## 3. 專案入口與架構

| 路徑 | 用途 |
|---|---|
| `index.html` | 可直接開啟的遊戲頁；載入角色、聲音 manifest、音效播放器，再載入 `game.js` |
| `game.js` | 遊戲狀態、職業卡池、戰鬥、敵人 AI、音訊、Canvas 場景、DOM 與輸入 |
| `styles.css` | HUD、卡片、門派與手機版面 |
| `character-art.js` | 四個角色的 Canvas 路徑與分部位動畫，公開 `window.CharacterArt` |
| `character-preview.html`、`character-preview.js` | 四角色動畫預覽；可切回原版造型 |
| `tests/character-art.test.cjs` | 不需外部套件的角色姿態與狀態測試 |
| `tests/boxer-basic.test.cjs` | 載入真實遊戲邏輯，以 DOM stub 測拳師、受擊位置及相容性 |
| `sounds/README.md` | 音效檔名與替換操作清單；不另記專案進度 |
| `combat-audio.js`、`sounds/manifest.js` | 預載、解碼、音色變體、聲部上限與音檔清單 |
| `scripts/generate-sounds.cjs` | 以固定種子重新產生完整 WAV 音效庫及 manifest，不需第三方套件或素材 |
| `tests/combat-audio.test.cjs`、`tests/combat-audio.browser.cjs` | 音檔與播放器單元測試、真實瀏覽器解碼／混音／技能對應測試 |
| `assets/characters/`、`character-sprites.js` | 未採用的貼圖實驗，現行 HTML 不載入，先前未上傳 |
| `docs/archive/` | 原始交接文件的唯讀歷史封存，非現行規格 |

- 沒有 bundler、`package.json`、lint 或 ES module 拆分；不要假設存在 `npm test`。
- `game.js` 仍共享全域 `state`；`cast(game)` 中也可能呼叫依賴全域狀態的函式，並非真正解耦。
- 棋盤每方 4×4，共 8 欄×4 列；邏輯畫布 1152×648，透視座標由 `boardPoint`、`cellPolygon`、`cellCenter` 統一處理。
- DPR backing store 上限 2.5；不要用裝置像素取代戰鬥邏輯座標。背景、棋盤有離屏快取。
- 遊戲迴圈以 `requestAnimationFrame` 驅動，dt 上限 0.033 秒；暫停時不執行 `update`，但仍繪圖。
- 繪圖順序：背景、棋盤與準心、底層特效、警示、深度排序角色、彈幕、前景特效、文字。命中特效必須在角色上方。

## 4. 現行玩法與數值

### 共通規則

- 手牌固定四槽；用掉一張只清空該槽，新牌補第一個空槽，不重排剩餘卡。允許抽到重複卡。
- 有目標的卡兩段施放：第一次選取預覽，第二次同卡才消耗。金絲軟甲、月弧返斬、運氣調息一次生效。
- 一般技能準心是玩家前方第 4 格；拳師普攻出招期間使用快照目標，返回後恢復一般準心。
- 玩家對敵人的傷害採 ±10% 浮動、15% 暴擊率、暴擊 2 倍；敵人攻擊使用各招既有傷害，不應誤寫成敵我共用暴擊。
- 傷害字依 basic／skill／big 分層；暴擊、特殊增傷以大字呈現，色值依目前 `INK` 與 `addDamageNumber`。
- **目前集氣先封頂 100，再扣 100 抽牌，超額會丟棄。** 例如拳師從 90 再命中一次，抽一張後為 0，不是 80。手牌滿可存 100，空出槽後兌換。
- 保留鍵盤移動、J／Space 普攻、1–4 技能、Q 取消、P／Escape 暫停。手機點棋盤普攻、滑動移動，點卡選取／施放；說明區可折疊。

### 職業與普攻

| 職業 | HP | 普攻基礎傷害 | 命中集氣 | 普攻節奏與命中 |
|---|---:|---:|---:|---|
| 唐門 `tangmen` | 135 | 12 | 24 | CD 0.34 秒，袖箭投射物命中 |
| 劍客 `swordsman` | 150 | 14 | 42 | CD 下限 0.16 秒；場上最多一道普攻劍氣，前一道命中或離場後才可再出手 |
| 拳師 `boxer` | 180 | 18 | 90 | CD 1 秒；同列鎖定、瞬移出拳後返回，詳見下節 |

**劍客普攻已是飛行劍氣，不是固定第 4 格傷害；流光劍氣「技能」才是瞬間整列攻擊。** 開場 `message` 尚有過期字樣，列入待辦。

### 拳師普攻：最新規格，不能退回歷史版本

- `boxerBasicTarget()`：敵人可見位置與玩家同一橫列且左側有合法格時，鎖定該敵人格；不同列則用原準心，不跨列追蹤。
- 出手保存 `origin`、`landing`、`aim` 快照。立即出現在目標左一格；敵方第 0 欄的左格是己方第 3 欄，不可負索引或越界。
- 0.16 秒揮拳命中點才以 `enemyOnCell(aim)` 判定；每次最多一次傷害與集氣。敵人提前離格可閃過，空揮不扣血、不集氣。
- 0.42 秒收招返回原格。出拳期間不接受移動、新普攻或施放卡片；百裂崩拳引導期間不啟動瞬移普攻。
- 暫停凍結出拳進度；戰鬥結束／死亡清理未完成出拳，重開不留下延遲攻擊。
- `state.player.col/row` 保存原位；**繪圖、受擊、敵人點名使用 `playerCombatCell()`／`playerCombatPosition()`／`playerOnCell()`**。不能只把角色畫走，碰撞卻留在原地。
- `offsetCell()` 處理跨陣營相鄰格；敵方區域波浪、彈幕與警示仍會傷到瞬移中的拳師，不額外給無敵。
- 對應函式：`playerBasicAttack`、`updateBoxerStrike`、`finishBoxerStrike`、`addDashTrail`。

### 卡池與控場

| 門派 | 現有卡牌 |
|---|---|
| 唐門，4 張 | 透骨釘 `bone-nail`、千機匣 `thousand-machine`、金絲軟甲 `silk-armor`、暴雨梨花針 `pear-blossom` |
| 劍客，4 張 | 流光劍氣 `sword-qi`、斷雲橫砍 `cross-cut`、追風突刺 `thrust`、月弧返斬 `moon-arc` |
| 拳師，6 張 | 震山推掌 `driving-palm`、擒龍勁 `dragon-pull`、鎖脈震擊 `meridian-lock`、百裂崩拳 `hundred-fist`、運氣調息 `breathing`、亢龍有悔 `dragon-regret` |

- 流光劍氣使敵人浮空 1 秒；接帶 `combo: true` 的斷雲橫砍才有聯招加倍、吹至敵方最後欄、緩速與短暈。不是所有攻擊都能接聯招。
- 追風突刺：準心格及左格，定身 4 秒，無聯招；月弧返斬：1.2 秒架勢，擋下下一次傷害後消耗架勢並反擊。
- 震山／擒龍以準心及左欄、上下列形成最多 2×3 範圍，推／拉一格並緩速；邊界會裁切。
- 鎖脈：準心及左格定身 4 秒。運氣調息抽兩張，受四槽容量限制。
- 百裂崩拳只打固定準心格，引導 1.6 秒、不能移動，有持續傷害與收尾重擊；收尾的時序仍需檢查，見待辦。
- 亢龍有悔延遲 2 秒、基礎傷害 92、必暴擊。`crossCells()` 目前是同陣營完整橫列＋直欄的 7 格十字，不是鄰接 5 格。
- `root` 只限制移動；`stun`、`airborne`、擊退才是敵人瞬身斬的中斷條件，不可混用。

### 敵人

- 魔道宗師 HP 600。場地攻擊與招式分軌，可重疊，不應用單一忙碌旗標全部停掉。
- 周天罡氣覆蓋外圈＋內圈 16 格；梅花樁陣交錯黑白格；沒有舊評估所說的固定內圈安全區。
- 橫江斷浪由敵人位置向前掃豎三格；漫天花雨留一列生路；普通彈幕有車道預警；反身崩罡針對長時間對位輸出。
- 瞬身斬到玩家前方，立即亮豎三格警示，1 秒後攻擊再返回；預警中遭暈眩、浮空或擊退會中斷，控場結束才傳回。

## 5. 美術、動畫與音訊契約

- 四造型：唐門玉青袖箭、劍客白袍長劍、拳師赤衣拳套、魔道銀髮赤黑甲。保留目前水墨場景；美術偏好以使用者試看為準。
- `CharacterArt.draw(ctx, character, unit, options)` 只繪圖，不改 HP、位置或命中；保存／恢復 Canvas 狀態。角色原點腳部約 `y=18`，玩家向右、敵人鏡射向左。
- 使用靜態 `Path2D` 快取、兩節手臂關節、分離的頭髮／衣襬／軀幹／腿／武器；不可恢復整個人橫向壓扁的舊出手做法。
- `play` 啟動動作、`update` 跟隨未暫停的遊戲時鐘、`sample` 決定姿態；控場、受擊、倒地會覆蓋一般出手，敵人舉刀與警示狀態對齊。
- `play` 預設不讓移動取消攻擊、普攻取消施法姿態；**拳師普攻入口目前會主動清除 `artAction` 再播放**，不能僅憑渲染器優先順序推斷戰鬥是否可中斷。
- `?art=classic` 只切回原繪圖，不切回舊玩法。`character-preview.html` 是動畫檢視頁，不使用生成貼圖。
- 使用者曾明確指出生成動作貼圖互相重疊／超框；未採用的 PNG 與 `character-sprites.js` 不得默默接回遊戲。
- 更動角色後檢查完整武器邊界、手機縮放、第一排頭部、血條、警示與命中特效圖層。瞄準訊息目前在場地下緣，避免蓋住頭部。
- 2026-09-06 改以完整預生成音檔為主：46 個聲音事件、72 個 44.1kHz／16-bit 立體聲 WAV，共約 5.76 MiB。這是原創 DSP／程序化擬音，不是實錄或外部 AI 音效服務產物。
- 劍系用短刃風／非整數金屬泛音，暗器用機簧／針雨／入體聲，拳系用拍擊／低頻衝擊，擒龍用反向吸氣；百裂收尾與亢龍爆發有獨立重擊。調息與抽牌分開，敵人波浪新增專屬聲音。
- 13 個高頻重複事件各有三個 WAV 變體，避免相鄰重複；蓄力等技能音固定速率，不隨機拉長 2 秒計時。WAV 有短反射，不再次送入舊長殘響；主壓縮器 ratio 4、threshold -10dB，保留強弱層次。
- 依職業選用劍擊／暗器／拳擊命中音；百裂與亢龍已有專屬聲音時不再疊通用重擊，暴擊仍可附加短 accent。這不改傷害計算或百裂原有 tick 時序。
- HTTP(S) 頁面以最多四個下載預載；首次觸控／鍵盤操作解鎖 Web Audio 並解碼。最多 20 聲部，同幀轟炸合併、警示／大招優先；暫停、靜音、重開、勝敗會停止 sample 尾音。暫停恢復不重播被停止的蓄力尾音，戰鬥計時仍照原邏輯凍結／恢復。
- 靜音偏好保留；`file://`、音檔缺失／解碼失敗保留舊即時合成後備，聲音失敗不阻止遊戲。舊合成後備不具有新 sample 的完整停止管理。
- 替換音效請改生成腳本配方與版本號，重跑腳本產生 WAV／manifest，並更新 `index.html` 的腳本版本 query；勿只改未列入 manifest 的檔案。

## 6. 已知問題與待辦，不代表已承諾實作

| 優先 | 狀態與問題 | 下一步與驗收 |
|---|---|---|
| P1 | 集氣溢出丟棄：已核對現碼與測試，沿用舊上限 | 先確認是否要保留餘額／儲存上限，再測滿手牌、補槽與多次抽牌；不可暗改 |
| P1 | 勝敗後 `updateProjectiles`／`updateEffects` 仍可執行，通用傷害入口沒有完整階段阻擋：靜態風險，尚未修 | 重現延遲技能與勝敗同幀；分開「結束後演出」與「禁止新增戰鬥傷害」 |
| P1 | 百裂崩拳在剩餘 ≤0.2 秒觸發重擊，但一般 tick 仍以 time>0 運作：有重擊後再跳小傷的時序風險 | 加逐幀傷害順序測試，驗收最後一筆確為重擊 |
| P1 | 劍客開場訊息仍寫固定第 4 格；部分技能用 `enemy.col/row`，其他用 `enemyVisualCell()` | 修文案前核對玩法；補敵人瞬移時「預覽／命中／特效」一致性測試 |
| P2 | `state.message` 仍被多處覆寫，重要提示可能被普攻訊息蓋掉 | 規劃少量分級通知，保留獨立瞄準條 |
| P2 | 手機滑一次只移一格、棋盤受縮放限制 | 真機驗證可讀性與操控，再決定連滑／方向控制，不直接改規則 |
| P2 | 新拳師同列鎖定＋90 集氣尚無新平衡評估 | 使用者試玩優先；建立可重現模擬，再看命中率、抽牌間隔與戰鬥長度 |
| P2 | 角色動畫與音檔播放器已獨立，戰鬥邏輯仍是單檔；已有角色、拳師、音效 Node 測試與音效瀏覽器測試 | 優先補戰鬥契約與技能測試，再逐步拆狀態、目標、傷害、AI；不一次重寫 |
| P3 | Hitstop、可關閉震屏、通用粒子、受擊白閃等仍是候選改進 | 先評估可讀性、效能與減少動態設定，不把歷史建議標成已完成 |
| P3 | 關卡、獎勵、擴充卡池、Boss 階段、進度存檔、卡牌 Cost 均未實作 | 都是新設計，另行確認；尤其 Cost 會改變普攻集氣抽牌核心循環 |

歷史評估中的「沒有角色動作／DPR／靜音／Boss 長血條／任何測試」「每次抽牌重建整手 DOM」「月弧可在一次架勢中無限反擊」已不適用，勿直接照舊 P0 清單重做。
卡池 4 張不表示四張手牌必定各不相同；舊文關於重複卡與模擬勝率的結論須另驗。

## 7. 驗證方式與證據

以下均為原生 Node 命令，不需安裝套件：

```sh
node --check game.js
node --check character-art.js
node --check character-preview.js
node tests/character-art.test.cjs
node tests/boxer-basic.test.cjs
node tests/combat-audio.test.cjs
git diff --check
```

- 2026-09-06 全音效測試通過：72 個 WAV 的 PCM 格式、起音、非靜音、峰值低於 0.92、DC／尾端淡出、無重複檔案；所有 SFX 呼叫都有 manifest。播放器測試覆蓋四併發預載、一次解碼、變體、同幀去重、固定蓄力速率、聲部上限、停止清理、載入失敗後備。
- 2026-09-06 以 Playwright＋Chrome 重跑 `tests/combat-audio.browser.cjs`：46 個事件真實解碼／播放，14 張卡招式對應正確；OfflineAudioContext 混戰峰值約 0.69、RMS 約 0.063，劍击高頻比例高於拳擊；暫停／重開／靜音、390px 觸控普攻通過，無 JS 錯誤。這是客觀音訊與互動驗證，不取代使用者聽感評價／真機喇叭測試。此測試需可解析 `playwright`；可用 `BROWSER_EXE` 指定 Chrome。
- 2026-09-06 既有拳師 15 項（64 組位置）與角色 24 動作測試重跑通過。
- 2026-09-05 文件整理後重跑當時既有檢查；結果以本次實際輸出為準，不代表完整遊戲無 bug。
- 2026-09-05 Codex 發佈前再次執行三個語法檢查、角色測試、拳師 15 項測試及 diff 檢查，全數通過。本次部署不重做先前的瀏覽器視覺測試。
- 2026-09-05 Claude 接手時重跑同一批命令：三個 `--check` 通過；角色測試「24 animation paths」通過；拳師測試 15 項（含 64 組同列位置）通過；`git diff --check` 無問題。未改任何程式碼。
- 角色測試：四角色×六動作、數值有限、結束清理、姿態優先、動畫狀態更新不修改戰鬥座標／HP。
- 拳師測試：15 項，含 64 組同列位置、跨邊界、不同列落空、目標離格、一次命中、90 集氣、既有氣條上限、輸入互斥、暫停、引導、死亡、返回、彈幕／場地碰撞及其他職業。
- 2026-09-05 拳師修改時曾以 Playwright 檢查桌機／390px 手機觸控、出拳與返回截圖、實際遊戲迴圈，未見 JS 錯誤；本次純文件整理不重做視覺測試。
- 2026-09-04 角色版曾檢查四造型像素非空與動作差異、武器邊界、14 張卡的姿態與暫停恢復；此紀錄不是未來新版本的保證。
- 2026-07-29 舊模型報告過 2816 組技能預覽比對、音訊離線／頻譜檢查及平衡模擬；對應完整測試腳本目前不在專案中，視為歷史報告，不能宣稱本次已重現。

## 8. 部署與版本

- 倉庫：<https://github.com/jojomadstar/GridBattleTEST>。
- 分享網址：<https://jojomadstar.github.io/GridBattleTEST/>；預覽：<https://jojomadstar.github.io/GridBattleTEST/character-preview.html>。
- GitHub Pages 從 `main` 根目錄發佈，純靜態檔案；本機開啟 HTML 不代表網路已更新。
- 2026-09-06 全音效版：已通過本機測試，使用者要求直接發佈；部署結果待本次上傳後核對。發佈前遠端為 `43af7a2`。
- 2026-09-05 已發佈遊戲版本 `e66bacf`，包含拳師普攻／90 集氣、回歸測試與統整文件；[Pages 工作 33984507595](https://github.com/jojomadstar/GridBattleTEST/actions/runs/33984507595) 成功。線上六個遊戲／預覽檔案、兩個模型入口與兩份封存均回應 HTTP 200 且內容一致。
- 發佈前的 `c89782e` 已保留在 `codex/backup-before-boxer-handoff-c89782e`；更早的 `a5f1550` 仍保留在 `codex/backup-before-canvas-art-a5f1550`。
- 此部署紀錄與 Claude 新增的接手紀錄以文件專用後續提交保存，不更動 `e66bacf` 的遊戲內容。
- 發佈前檢查工作樹、測試與必要檔案；不要把 `.claude/` 私人設定、憑證或未採用貼圖一起批次加入。
- 發佈後確認對應 commit 的 Pages 工作成功、線上必要資源與本機一致，才更新本節。新入口與封存文件也應納入文件備份，不再漏掉交接紀錄。

## 9. 精簡更新記錄

| 日期／來源 | 已完成內容 | 取代的舊決策／限制 |
|---|---|---|
| 2026-09-06，Codex | 重製 46 個音效／72 WAV、產生器、分職業命中路由、聲部管理、快取版本與音效測試 | 取代「沒有實際音檔、全部靠即時合成」；不調整戰鬥數值。使用者授權直接部署，結果見第 8 節 |
| 2026-09-05，Codex，依使用者上傳要求 | 已提交並部署 `e66bacf`；備份前版 `c89782e`，通過測試與線上檔案核對，保留 Claude 新增的文件紀錄 | 下列「未推送／本機未部署」是各次工作結束當時的歷史狀態；本批現已發佈。未採用素材與私人設定未上傳 |
| 2026-09-05，Claude | 依使用者指示接受本文件為唯一核心；重跑 §7 全部檢查（全數通過）並抽查文件與原始碼一致性：HP 135/150/180/600、拳師 CD 1 秒／集氣 90／傷害 18、唐門 24、劍客 42、`gainCharge` 確實封頂丟棄溢出、`crossCells` 確為 7 格、劍客開場文案與 `update()` 未擋階段兩項 P1 確實仍存在——皆與本文相符 | 未改任何程式碼、未推送；我方 2026-07-29 第八輪拳師數值維持「已被取代」狀態 |
| 2026-09-05，Codex | 統整為本文件；兩份原文封存，增加模型入口、現況／待辦／驗證／部署分類 | 不再分別維護角色說明與長篇評估；未修改玩法、未推送 |
| 2026-09-05，Codex，依最新使用者指示 | 拳師只在同列鎖定，出現在敵人左格出拳後返回；命中集氣 45→90；新增位置與碰撞回歸測試 | 取代跨列必中、改變永久站位、集氣 45；也取代中途提出的完全手動準心方案；本機未部署 |
| 2026-09-04，Codex | 四角色 Canvas 分部位造型與動畫、比較頁、拳師舊 squash／重複插值修正、瞄準文字避開角色 | 不採用生成 PNG 動作表；已部署 `c89782e` |
| 2026-07-29，其他模型歷史紀錄 | 水墨場景、DPR／快取、增量手牌、雙軌敵人、唐門、飛行劍氣普攻、兩段施法、音訊重做 | 已核對主要功能存在；舊數值／模擬不當作現行標準 |
| 2026-07-29，第八輪，已被取代 | 拳師曾跨列移到敵人鏡像格、必中，採 1 秒 CD／集氣 45／傷害 18／僵直 0.22 秒 | 當時是為平衡必中而放慢；不是最新使用者要求，勿恢復 |

## 10. 原文封存

- [舊設計評估與八輪更新原文](docs/archive/design-review-20260729.txt)：完整保留分析、數值實驗、音效量測、建議參數與架構提案。
- [合併前角色技術文件](docs/archive/character-art-before-merge-20260905.txt)：保留原 API、測試與預覽說明。

封存是查來源用，不繼續更新，也不要求每次全讀。日常交接只讀本文件；需追溯原因時才查原文，避免重複消耗上下文。
